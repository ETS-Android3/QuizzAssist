class Teacher {
    string studentGrade;
    void moveStudent(int x, int y) {
        //move the student to {x, y}
    int x = 5;
    }

    int evaluateGradePercentage(char letterGrade) {
        studentGrade == letterGrade;
    }
};

